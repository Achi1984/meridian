const API_BASE=(window.MERIDIAN_V9_CONFIG?.apiBase||'https://p01--achi-meridian--ttvk44grdlp7.code.run').replace(/\/$/,'');
const TOKEN_KEY='meridian.v8.readToken';
const FALLBACK=[
{id:'BTC-7X',symbol:'BTC',leverage:7,lower:55000,upper:100000,be:85907.2,liq:62001.6,tp:100000,investCoin:.03,price:85704.2,createdPrice:85866,grids:174},
{id:'ETH-3X-A',symbol:'ETH',leverage:3,lower:1750.02,upper:3900,be:2623.5,liq:1694.32,tp:3900,investCoin:.2518},{id:'ETH-4X-B',symbol:'ETH',leverage:4,lower:1750.02,upper:3500.05,be:2632.87,liq:1783.99,tp:3500.05,investCoin:.3405},
{id:'SOL-4X',symbol:'SOL',leverage:4,lower:55,upper:185,be:115.655,liq:66.51,tp:185,investCoin:58.807,price:115.553,createdPrice:115.6,grids:374},
{id:'XRP-4X',symbol:'XRP',leverage:4,lower:.85,upper:1.8501,be:1.4442,liq:.9163,tp:1.8501,investCoin:1868.17},
{id:'HBAR-5X-A',symbol:'HBAR',leverage:5,lower:.06,upper:.12,be:.08569,liq:.0606,tp:.12,investCoin:10800,dynamicMargin:2200},{id:'HBAR-5X-B',symbol:'HBAR',leverage:5,lower:.06,upper:.125,be:.08622,liq:.06343,tp:.125,investCoin:5000},{id:'HBAR-3X-C',symbol:'HBAR',leverage:3,lower:.05,upper:.15,be:.08682,liq:.05198,tp:.15,investCoin:7990.15},
{id:'SUI-LONG-4X',symbol:'SUI',side:'LONG',leverage:4,lower:.6,upper:1.5,be:1.0196,liq:.6543,tp:1.5,investCoin:1365.2,price:.9686,createdPrice:1.0375,grids:289,pnlCoin:-75.89,profitPct:-5.56},
{id:'ADA-LONG-5X',symbol:'ADA',side:'LONG',leverage:5,lower:.125,upper:.5,be:.2572,liq:.1616,tp:.5,investCoin:5891.35,price:.2398,createdPrice:.259,grids:482,profitPct:-8.33},
{id:'PEPE-4X-A',symbol:'PEPE',leverage:4,lower:.000002,upper:.000006,be:.0000040029,liq:.000002368,tp:.000006,investCoin:73524672.71},{id:'PEPE-3X-B',symbol:'PEPE',leverage:3,lower:.000002,upper:.000006,be:.0000040029,liq:.0000021748,tp:.000006,investCoin:31670289.08},
{id:'AVAX-LONG-6X',symbol:'AVAX',side:'LONG',leverage:6,lower:6,upper:14.5,be:10.923,liq:7.226,tp:14.5,investCoin:90.46,dynamicMargin:9.79,price:10.326,createdPrice:11.189,grids:253,profitPct:-7.16},
{id:'DOT-SHORT-5X',symbol:'DOT',side:'SHORT',leverage:5,lower:.7,upper:2,be:1.195,liq:1.604,tp:.7,investCoin:244.66,price:1.099,createdPrice:1.188,grids:364,profitPct:24.18},
{id:'ADA-SHORT-3X',symbol:'ADA',side:'SHORT',leverage:3,lower:.125,upper:.5,be:.2596,liq:.477,tp:.125,investCoin:1443.37,price:.2398,createdPrice:.2587,grids:483,profitPct:13.54},
{id:'SUI-SHORT-4X',symbol:'SUI',side:'SHORT',leverage:4,lower:.75,upper:2,be:1.0148,liq:1.7314,tp:.75,investCoin:455.06,price:.9681,createdPrice:1.0077,grids:413,profitPct:6.22},
{id:'AVAX-SHORT-6X',symbol:'AVAX',side:'SHORT',leverage:6,lower:8,upper:20,be:11.052,liq:17.246,tp:8,investCoin:8.3,price:10.326,createdPrice:10.908,grids:366,profitPct:10.5},
{id:'LINK-LONG-5X',symbol:'LINK',side:'LONG',leverage:5,lower:6.5,upper:21,be:12.707,liq:8.178,tp:21,investCoin:147.37,price:12.292,createdPrice:12.734,grids:379,profitPct:-3.69},
{id:'LINK-SHORT-4X',symbol:'LINK',side:'SHORT',leverage:4,lower:8,upper:25,be:12.737,liq:19.959,tp:8,investCoin:36.84,price:12.292,createdPrice:12.72,grids:445,profitPct:7.03},
{id:'XLM-LONG-5X',symbol:'XLM',side:'LONG',leverage:5,lower:.12,upper:.36001,be:.21304,liq:.14327,tp:.36001,investCoin:5430.54,price:.20386,createdPrice:.21364,grids:373,profitPct:-6.09},
{id:'XLM-SHORT-5X',symbol:'XLM',side:'SHORT',leverage:5,lower:.145,upper:.4,be:.2141,liq:.32662,tp:.145,investCoin:1357.63,price:.20386,createdPrice:.21345,grids:398,profitPct:9.33},
{id:'TRX-LONG-6X',symbol:'TRX',side:'LONG',leverage:6,lower:.25,upper:.44,be:.3417,liq:.26389,tp:.44,investCoin:3533.43,price:.33943,createdPrice:.34163,grids:185,profitPct:-1.32},
{id:'TRX-SHORT-5X',symbol:'TRX',side:'SHORT',leverage:5,lower:.275,upper:.45,be:.34139,liq:.45975,tp:.275,investCoin:706.68,price:.33943,createdPrice:.34154,grids:170,profitPct:1.37},
{id:'WIF-LONG-6X',symbol:'WIF',side:'LONG',leverage:6,lower:.1,upper:.45,be:.2418,liq:.1385,tp:.45,investCoin:2042.24,price:.2275,createdPrice:.2432,grids:480,profitPct:-5.09},
{id:'WIF-SHORT-6X',symbol:'WIF',side:'SHORT',leverage:6,lower:.1,upper:.5,be:.2441,liq:.3131,tp:.135,investCoin:408.44,price:.2275,createdPrice:.2435,grids:546,profitPct:27.92}
];
const HEDGES=[
{id:'BTC-HEDGE-PIONEX-20X',venue:'Pionex',symbol:'BTC',side:'SHORT',leverage:20,investCoin:.00818,entry:85098.8,be:85080.1,liq:105327.4,sl:null,lower:70990.5,upper:106485.7,tp:70990.5,dynamicMargin:.01537,price:85780.9,grids:181,note:'BTC Short Grid'},
{id:'BTC-HEDGE-OKX-7X',venue:'OKX',symbol:'BTC',side:'SHORT',leverage:7,investCoin:.0116,entry:85990.6,be:85990.6,liq:97591.7,sl:97500,price:86016.6,tps:[{price:84600,coin:.0029},{price:81800,coin:.0034},{price:74300,coin:.0029}],runnerCoin:.0024,note:'TP1 → SL auf Entry ~85.990–86.050'}
];
const HEDGE=HEDGES[0];
const state={bots:FALLBACK,hedge:HEDGE,hedges:HEDGES,source:'REFERENCE',market:null,intel:null,assetIntel:{},portfolio:null,error:null,manual:{pionex:23326.51,bitpanda:5408.27,ledger:776.74,okx:138.21}};
const $=s=>document.querySelector(s),num=v=>Number.isFinite(Number(v))?Number(v):null;
const money=x=>{x=num(x);if(x==null)return'—';if(x<.001)return'$'+x.toPrecision(5);return'$'+x.toLocaleString('de-DE',{maximumFractionDigits:2})};
function token(){try{return String(localStorage.getItem(TOKEN_KEY)||'').trim()}catch{return''}}
async function getJson(path){const r=await fetch(API_BASE+path,{cache:'no-store',headers:{accept:'application/json',...(token()?{authorization:'Bearer '+token()}:{})}});if(!r.ok)throw new Error('HTTP '+r.status);return r.json()}
function holdingValue(d,h){const q=num(h?.quantity),lp=num(d?.livePrices?.[h?.symbol]?.price),own=num(h?.price),stored=num(h?.value)??num(h?.valueUsd)??num(h?.usdValue);return q!=null&&q>=0&&lp>0?q*lp:q!=null&&q>=0&&own>0?q*own:stored||0}
function portfolioModel(d){const hs=Array.isArray(d?.portfolio?.holdings)?d.portfolio.holdings:[];const by={};for(const h of hs){const venue=String(h?.venue||'OTHER').toUpperCase(),v=holdingValue(d,h);by[venue]=(by[venue]||0)+v}const liveBots=Array.isArray(d?.pionexRisk?.bots)?d.pionexRisk.bots.map(normalizeLive):[];const botCapital=liveBots.reduce((s,b)=>s+(num(b.invest)||0)+(num(b.pnl)||0),0);const apiPionex=num(d?.pionex?.equityUsd)??num(d?.pionex?.totalEquityUsd)??num(d?.pionex?.accountValueUsd)??num(d?.pionexRisk?.accountEquityUsd)??num(d?.pionexRisk?.totalEquityUsd);const botRowsWithCapital=liveBots.filter(b=>b.invest!=null);const allBotsHaveCapital=liveBots.length>0&&botRowsWithCapital.length===liveBots.length;/* The current private API equity field is COIN-M bot equity, not Pionex account total.
   Never let that partial figure overwrite the verified account-total snapshot. */
const verifiedPionexTotal=state.manual.pionex;
const apiLooksLikeAccountTotal=apiPionex!=null&&apiPionex>=verifiedPionexTotal*.8;
const pionex=apiLooksLikeAccountTotal?apiPionex:verifiedPionexTotal;
const bitpanda=state.manual.bitpanda,ledger=state.manual.ledger,okx=state.manual.okx;const total=pionex+bitpanda+ledger+okx;return{total,pionex,bitpanda,ledger,okx,botCapital:allBotsHaveCapital?botCapital:null,pionexComplete:true,pionexSource:apiLooksLikeAccountTotal?'PRIVATE_ACCOUNT_TOTAL':'SCREENSHOT_TOTAL'}}
function pick(b,keys){for(const k of keys){const v=num(b?.[k]);if(v!=null)return v}return null}function normalizeLive(b){const nested=b?.bot||b?.position||b?.data||{};const x={...nested,...b};return{id:String(x.id||x.botId||x.bot_id||x.name||x.symbol||'BOT'),symbol:String(x.symbol||x.asset||x.baseAsset||x.base_asset||'').replace(/[-_/]?(USDT|USDC|USD)$/,'').toUpperCase(),leverage:pick(x,['leverage','leverageX','leverage_x']),lower:pick(x,['lowerRange','rangeLower','lowerPrice','lower_price','minPrice','min_price']),upper:pick(x,['upperRange','rangeUpper','upperPrice','upper_price','maxPrice','max_price']),be:pick(x,['breakEvenPrice','breakevenPrice','break_even_price','avgEntryPrice','averageEntryPrice']),liq:pick(x,['pionexLiquidationPrice','liquidationPrice','liqPrice','liquidation_price']),tp:pick(x,['takeProfit','tpPrice','tp','take_profit_price']),price:pick(x,['currentPrice','price','markPrice','lastPrice','mark_price','last_price']),buffer:pick(x,['pionexLiqBufferPct','liqBufferPct','liquidationDistancePct']),pnl:pick(x,['totalProfitUsd','totalProfitUSDT','totalProfitUsdt','totalProfit','total_profit','total_profit_usdt','pnlUsd','unrealizedPnlUsd','unrealizedPnl','unrealized_profit','profit']),invest:pick(x,['investmentUsd','investmentUSDT','investmentUsdt','investment','investedUsd','invested','investment_usdt','invested_amount','initialInvestment','initial_investment']),profitPct:pick(x,['totalProfitPct','profitPct','pnlPct','total_profit_pct','profit_rate','profitRate']),side:String(x.side||x.direction||x.positionSide||'LONG').toUpperCase()}}
function relDiff(a,b){a=num(a);b=num(b);if(!(a>0&&b>0))return null;return Math.abs(a-b)/Math.max(Math.abs(a),Math.abs(b),1e-12)}
function botMatchScore(ref,x){
  if(x.symbol!==ref.symbol||x.side!==(ref.side||'LONG'))return 1e9;
  let score=0,signals=0;
  if(x.leverage!=null){score+=Math.abs(x.leverage-ref.leverage)*12;signals++}
  for(const [k,w] of [['lower',30],['upper',30],['tp',24],['be',10],['liq',6]]){
    const d=relDiff(x[k],ref[k]);if(d!=null){score+=Math.min(d,2)*w;signals++}
  }
  return score+(signals?0:500);
}
function mergeReference(live){
  const used=new Set,refs=FALLBACK.map(ref=>({...ref})),matches=[];
  for(let ri=0;ri<refs.length;ri++)for(let li=0;li<live.length;li++){
    const score=botMatchScore(refs[ri],live[li]);if(score<1e9)matches.push({ri,li,score});
  }
  matches.sort((a,b)=>a.score-b.score);
  const refUsed=new Set;
  for(const m of matches){
    if(refUsed.has(m.ri)||used.has(m.li))continue;
    const ref=refs[m.ri],x=live[m.li];
    /* Reject weak duplicate-coin guesses: live row must agree on leverage or a structural field. */
    const levOk=x.leverage!=null&&x.leverage===ref.leverage;
    const structureOk=['lower','upper','tp','be'].some(k=>{const d=relDiff(x[k],ref[k]);return d!=null&&d<.03});
    if(!levOk&&!structureOk)continue;
    refs[m.ri]={...ref,...Object.fromEntries(Object.entries(x).filter(([,v])=>v!=null&&v!=='')),side:ref.side||'LONG'};
    refUsed.add(m.ri);used.add(m.li);
  }
  /* Dashboard is intentionally pinned to the verified 13-bot COIN-M roster.
     Unmatched API rows are often stale/partial bot records and must not create phantom cards.
     Live values are allowed to enrich a verified reference bot only after a confident match. */
  const extras=live.filter((_,i)=>!used.has(i));
  return refs.concat(extras);
}

function ema(v,p){if(v.length<p)return null;const k=2/(p+1);let e=v.slice(0,p).reduce((a,b)=>a+b,0)/p;for(let i=p;i<v.length;i++)e=v[i]*k+e*(1-k);return e}
function rsi(v,p=14){if(v.length<p+1)return null;let g=0,l=0;for(let i=1;i<=p;i++){const d=v[i]-v[i-1];g+=Math.max(0,d);l+=Math.max(0,-d)}g/=p;l/=p;for(let i=p+1;i<v.length;i++){const d=v[i]-v[i-1];g=(g*(p-1)+Math.max(0,d))/p;l=(l*(p-1)+Math.max(0,-d))/p}return l?100-100/(1+g/l):100}
function macd(v){if(v.length<35)return null;const series=p=>{const k=2/(p+1),o=[v[0]];for(let i=1;i<v.length;i++)o.push(v[i]*k+o[i-1]*(1-k));return o};const a=series(12),b=series(26),m=v.map((_,i)=>a[i]-b[i]);const k=2/10;let s=m[0];for(let i=1;i<m.length;i++)s=m[i]*k+s*(1-k);return{line:m.at(-1),signal:s,hist:m.at(-1)-s}}
function atr(rows,p=14){if(rows.length<p+1)return null;const t=[];for(let i=1;i<rows.length;i++)t.push(Math.max(rows[i].high-rows[i].low,Math.abs(rows[i].high-rows[i-1].close),Math.abs(rows[i].low-rows[i-1].close)));let a=t.slice(0,p).reduce((x,y)=>x+y,0)/p;for(let i=p;i<t.length;i++)a=(a*(p-1)+t[i])/p;return a}
async function marketKlines(interval,limit,symbol='BTC'){const bar=interval==='4h'?'4H':interval==='1d'?'1D':interval.toUpperCase();try{const u='https://www.okx.com/api/v5/market/candles?instId='+symbol+'-USDT-SWAP&bar='+bar+'&limit='+Math.min(limit,300),r=await fetch(u,{cache:'no-store'});if(!r.ok)throw new Error('OKX '+r.status);const j=await r.json();if(j.code!=='0'||!j.data?.length)throw new Error('OKX data');return j.data.map(x=>({openTime:+x[0],high:+x[2],low:+x[3],close:+x[4],closeTime:+x[0]})).reverse()}catch(e){const r=await fetch('https://api.binance.com/api/v3/klines?symbol='+symbol+'USDT&interval='+interval+'&limit='+limit,{cache:'no-store'});if(!r.ok)throw e;return(await r.json()).map(x=>({openTime:+x[0],high:+x[2],low:+x[3],close:+x[4],closeTime:+x[6]}))}}
function intel(rows4,rows1){const c4=rows4.map(x=>x.close),c1=rows1.map(x=>x.close),p=c4.at(-1),R4=rsi(c4),R1=rsi(c1),M4=macd(c4),M1=macd(c1),A=atr(rows4),e20=ema(c1,20),e50=ema(c1,50),e100=ema(c1,100),e200=ema(c1,200);const w=rows4.slice(-90),lo=Math.min(...w.map(x=>x.low)),hi=Math.max(...w.map(x=>x.high)),span=hi-lo,fibs=[.382,.5,.618,.786].map(f=>({f,price:hi-span*f})),near=fibs.reduce((a,b)=>Math.abs(b.price-p)<Math.abs(a.price-p)?b:a);const dailyBull=p>e200&&e20>e50,score=(dailyBull?30:0)+(R4>=42&&R4<=68?15:0)+(M4&&M4.hist>0?15:0)+(R1>=40&&R1<=70?10:0)+(M1&&M1.hist>0?10:0)+(Math.abs(p-near.price)<=Math.max(A*.75,p*.012)?20:0);return{price:p,rsi4:R4,rsi1:R1,macd4:M4,macd1:M1,atr:A,ema20:e20,ema50:e50,ema100:e100,ema200:e200,lo,hi,near,score,status:score>=70&&dailyBull?'RE-ENTRY READY':'WAIT FOR RETRACE'}}
function profitLockIntel(rows4,rows1){const c4=rows4.map(x=>x.close),c1=rows1.map(x=>x.close),R4=rsi(c4),R1=rsi(c1),M4=macd(c4),M1=macd(c1),p=c4.at(-1),e20=ema(c1,20),e50=ema(c1,50);let bearish=0,reasons=[];if(R4>=70){bearish++;reasons.push('RSI4H')}if(R1>=72){bearish++;reasons.push('RSI1D')}if(M4&&M4.hist<0){bearish++;reasons.push('MACD4H')}if(M1&&M1.hist<0){bearish++;reasons.push('MACD1D')}if(e20&&p<e20){bearish++;reasons.push('EMA20')}if(e20&&e50&&e20<e50){bearish++;reasons.push('EMA20/50')}const action=bearish>=4?'RISK REVIEW':bearish>=3?'PROFIT LOCK CANDIDATE':bearish>=2?'WATCH PROFIT':'HOLD';return{action,bearish,reasons,rsi4:R4,rsi1:R1,macd4:M4,macd1:M1}}
async function syncIntel(){try{const [h4,d1]=await Promise.all([marketKlines('4h',240),marketKlines('1d',240)]);state.intel=intel(h4,d1)}catch(e){state.intel=null}
const assets=[...new Set(state.bots.map(b=>b.symbol))];const out={};await Promise.all(assets.map(async s=>{try{const [h4,d1]=await Promise.all([marketKlines('4h',120,s),marketKlines('1d',220,s)]);out[s]=profitLockIntel(h4,d1)}catch{}}));state.assetIntel=out}

function risk(b){const p=num(b.price)||num(b.be),liq=num(b.liq);if(!(p>0&&liq>0))return null;return b.side==='SHORT'?(liq-p)/p*100:(p-liq)/p*100}
function tpDist(b){const p=num(b.price)||num(b.be),tp=num(b.tp);if(!(p>0&&tp>0))return null;return b.side==='SHORT'?(p-tp)/p*100:(tp-p)/p*100}
function status(b){const r=risk(b),t=tpDist(b);if(t!=null&&t<=5)return['TP ZONE','tp'];if(r==null)return['CHECK','muted'];if(r<5)return['URGENT','danger'];if(r<10)return['MARGIN','danger'];if(r<18)return['WATCH','watch'];return['SAFE','safe']}
function botCard(b){const [st,tone]=status(b),r=risk(b),t=tpDist(b),pct=b.profitPct!=null?b.profitPct:(b.invest>0&&b.pnl!=null?b.pnl/b.invest*100:null),pi=state.assetIntel[b.symbol],pa=pi?.action||'SYNC',pt=pa==='LOCK 2'?'danger':pa==='LOCK 1'?'watch':pa==='WATCH PROFIT'?'watch':'safe';return `<article class="bot bot-v2"><div class="bothead"><div><h3>${b.symbol}</h3><small>${b.side||'LONG'} · ${b.leverage||'—'}x</small></div><span class="tag tone-${tone}">${st}</span></div><div class="profit-lock"><span>PROFIT LOCK</span><b class="tone-${pt}">${pa}</b><small>${pi?.reasons?.length?pi.reasons.join(' · '):'4h/1D Momentum'}</small></div><div class="profit-row"><div><span>INVEST</span><b>${b.invest==null?'—':money(b.invest)}</b></div><div><span>GESAMTPROFIT</span><b class="${b.pnl>0?'profit-pos':b.pnl<0?'profit-neg':''}">${b.pnl==null?'—':money(b.pnl)}${pct==null?'':' · '+(pct>=0?'+':'')+pct.toFixed(1)+'%'}</b></div></div><div class="bot-details"><span>PRICE <b>${money(b.price)}</b></span><span>BE <b>${money(b.be)}</b></span><span>LIQ <b>${money(b.liq)}</b></span><span>TP <b>${money(b.tp)}</b></span></div><div class="range-line">RANGE <b>${money(b.lower)} – ${money(b.upper)}</b></div><div class="risk"><i style="width:${Math.max(2,Math.min(100,r||0)*3)}%"></i></div><small class="distance">${r==null?'Liq —':r.toFixed(1)+'% Liq'} · ${t==null?'TP —':Math.max(0,t).toFixed(1)+'% TP'}</small></article>`}
function critical(){return [...state.bots].sort((a,b)=>(risk(a)??999)-(risk(b)??999))[0]}
function beDistance(b){const p=num(b.price)||num(b.be),be=num(b.be);return p>0&&be>0?(p-be)/be*100:null}
function assetExposureShare(symbol){const e=exposureModel(),x=state.bots.filter(b=>b.symbol===symbol).reduce((s,b)=>{const p=num(b.price)||num(b.be),coin=num(b.investCoin),usd=num(b.invest);return s+(coin!=null&&p>0?coin*p:(usd||0))},0);return e.longUsd>0?x/e.longUsd*100:0}
function riskV2(b){const liq=risk(b),be=beDistance(b),share=assetExposureShare(b.symbol),mom=state.assetIntel[b.symbol];let score=0,reasons=[];if(liq!=null&&liq<18){score+=3;reasons.push('LIQ')}else if(liq!=null&&liq<28){score+=2;reasons.push('Liq')}else if(liq!=null&&liq<38){score+=1;reasons.push('liq')}if((b.leverage||0)>=7){score+=2;reasons.push('Hebel')}else if((b.leverage||0)>=5){score+=1;reasons.push('hebel')}if(share>=25){score+=3;reasons.push('Konzentration')}else if(share>=15){score+=2;reasons.push('Exposure')}else if(share>=10){score+=1;reasons.push('exposure')}if(be!=null&&be<-8){score+=2;reasons.push('unter BE')}else if(be!=null&&be<0){score+=1;reasons.push('BE')}if(mom?.action==='RISK REVIEW'){score+=3;reasons.push('Momentum')}else if(mom?.action==='PROFIT LOCK CANDIDATE'){score+=2;reasons.push('Momentum')}else if(mom?.action==='WATCH PROFIT'){score+=1;reasons.push('Momentum')}const label=score>=8?'RISK REVIEW':score>=5?'WATCH':score>=3?'ATTENTION':'HOLD',tone=score>=8?'danger':score>=5?'watch':score>=3?'watch':'safe';return{score,label,tone,reasons,liq,be,share}}

function exposureModel(){const longs=state.bots.map(b=>{const px=num(b.price)||num(b.be),coin=num(b.investCoin),usd=num(b.invest);return{...b,notional:coin!=null&&px>0?coin*px:(usd||0)}});const longUsd=longs.reduce((s,b)=>s+b.notional,0),btcPx=num(state.bots.find(b=>b.symbol==='BTC')?.price)||num(state.hedge.entry),hedgeUsd=(state.hedges||[state.hedge]).reduce((s,h)=>s+(num(h.investCoin)||0)*(btcPx||num(h.price)||num(h.entry)||0),0),coverage=longUsd>0?hedgeUsd/longUsd*100:0;return{longUsd,hedgeUsd,netUsd:Math.max(0,longUsd-hedgeUsd),coverage}}
function portfolioRegime(){const e=exposureModel(),i=state.intel;let points=0,reasons=[];if(i){if(i.price>i.ema20&&i.ema20>i.ema50){points+=2;reasons.push('BTC > EMA20/50')}else if(i.price<i.ema20){points-=2;reasons.push('BTC < EMA20')}if(i.macd4?.hist>0){points++;reasons.push('4h MACD +')}else if(i.macd4?.hist<0){points--;reasons.push('4h MACD -')}if(i.rsi4>=70){points--;reasons.push('4h RSI heiß')}else if(i.rsi4>=50){points++;reasons.push('4h RSI >50')}}const avgRisk=state.bots.length?state.bots.reduce((s,b)=>s+riskV2(b).score,0)/state.bots.length:0;if(avgRisk>=6){points-=2;reasons.push('Bot-Risiko hoch')}else if(avgRisk>=4){points--;reasons.push('Bot-Risiko erhöht')}const regime=points>=2?'RISK-ON':points<=-2?'DEFENSIVE':'NEUTRAL';const hedge=e.coverage>=15?'AUSREICHEND':e.coverage>=7?'MITTEL':'KLEIN';const tone=regime==='RISK-ON'?'safe':regime==='DEFENSIVE'?'danger':'watch';return{regime,hedge,tone,points,avgRisk,reasons}}
function regimeStrip(){const r=portfolioRegime();return `<section class="regime-strip"><div><span>PORTFOLIO REGIME</span><b class="tone-${r.tone}">${r.regime}</b></div><div><span>HEDGE</span><b>${r.hedge}</b></div><div><span>AVG RISK</span><b>${r.avgRisk.toFixed(1)}</b></div><small>${r.reasons.slice(0,4).join(' · ')||'Market sync'}</small></section>`}
function exposureStrip(){const e=exposureModel(),tone=e.coverage>=15?'safe':e.coverage>=7?'watch':'danger';return `<section class="exposure-strip"><div><span>LONG EXPOSURE</span><b>${money(e.longUsd)}</b></div><div><span>BTC HEDGE</span><b>${money(e.hedgeUsd)}</b></div><div><span>NET LONG</span><b>${money(e.netUsd)}</b></div><div><span>HEDGE COVERAGE</span><b class="tone-${tone}">${e.coverage.toFixed(1)}%</b></div></section>`}
function command(){const c=critical(),cs=c?status(c):['CHECK'],p=state.portfolio||{total:0,pionex:0,bitpanda:state.manual.bitpanda,ledger:state.manual.ledger,okx:state.manual.okx},tp=[...state.bots].filter(b=>{const x=tpDist(b);return x!=null&&x<=10});return `<section class="portfolio-hero"><div><span>GESAMTPORTFOLIO</span><strong>${p.total!=null?money(p.total):'PIONEX SYNC'}</strong><small>Pionex + Bitpanda + Ledger + OKX Bot</small></div><div class="source-grid"><div><span>PIONEX</span><b>${p.pionex!=null?money(p.pionex):'SYNC'}</b><small>${p.pionexSource==='PRIVATE_ACCOUNT_TOTAL'?'Live Gesamtvermögen':'Gesamtvermögen · Screenshot 16:49'}</small></div><div><span>BITPANDA</span><b>${money(p.bitpanda)}</b><small>Spot / Earn</small></div><div><span>LEDGER</span><b>${money(p.ledger)}</b><small>Wallet</small></div><div><span>OKX</span><b>${money(p.okx)}</b><small>1 BTC DCA Bot</small></div></div></section>${regimeStrip()}${exposureStrip()}<div class="grid compact-grid"><section class="metric"><span>PIONEX BOTS</span><b>${state.bots.length}</b><small>COIN-M Long + Short · + BTC Hedge</small></section><section class="metric"><span>RISK</span><b class="tone-${c?status(c)[1]:'muted'}">${c?c.symbol+' '+cs[0]:'—'}</b><small>${c&&risk(c)!=null?risk(c).toFixed(1)+'% Liq':'Sync'}</small></section><section class="metric"><span>TP RADAR</span><b>${tp.length}</b><small>≤10%</small></section><section class="metric"><span>BTC NEXT RANGE</span><b class="wait">${state.intel?state.intel.score+'/100':'SYNC'}</b><small>${state.intel?.status||'loading'}</small></section></div><section class="action compact-action"><span>NEXT ACTION</span><b>${c&&risk(c)!=null&&risk(c)<10?c.symbol+' Liquidationspuffer prüfen':'Bots halten · kein FOMO-Re-Entry'}</b></section><div class="section-title"><h2>BOT PULSE</h2><small>kritischste zuerst</small></div><div class="bots">${[...state.bots].sort((a,b)=>(risk(a)??999)-(risk(b)??999)).slice(0,6).map(botCard).join('')}</div>`}
function botGroup(title,subtitle,items){if(!items.length)return '';return `<section class="bot-group"><div class="group-head"><div><h2>${title}</h2><small>${subtitle}</small></div><b>${items.length}</b></div><div class="bots">${items.map(botCard).join('')}</div></section>`}
function hedgeCard(){const hs=state.hedges||[state.hedge];return hs.map(h=>{const tp=(h.tps||[{price:h.tp,pct:100}]).map(x=>money(x.price)+' · '+(x.pct!=null?x.pct+'%':x.coin+' BTC')).join(' / '),runner=h.runnerPct!=null?h.runnerPct+'%':h.runnerCoin!=null?h.runnerCoin+' BTC':'—';return `<section class="bot-group hedge-group"><div class="group-head"><div><h2>BTC HEDGE · ${h.venue||'Pionex'}</h2><small>separate Versicherung · nicht als Long-Bot gezählt</small></div><b>SHORT ${h.leverage}x</b></div><article class="bot bot-v2"><div class="bot-details"><span>ENTRY <b>${money(h.entry)}</b></span><span>SL <b>${money(h.sl)}</b></span><span>LIQ <b>${money(h.liq)}</b></span><span>SIZE <b>${h.investCoin} BTC</b></span></div><div class="range-line">TP <b>${tp}</b></div><div class="profit-lock"><span>RUNNER</span><b class="tone-safe">${runner}</b><small>${h.note||''}</small></div></article></section>`}).join('')}
function assetGroup(symbol,items){const exposure=items.reduce((s,b)=>s+(num(b.invest)||0),0),worst=Math.min(...items.map(b=>risk(b)??999)),rv=items.map(riskV2).sort((a,b)=>b.score-a.score)[0];return botGroup(symbol+' · '+items.length+' BOT'+(items.length===1?'':'S')+' · '+rv.label,(exposure?money(exposure)+' live capital · ':'')+(worst<999?worst.toFixed(1)+'% Liq · ':'')+rv.share.toFixed(1)+'% Exposure · Risk '+rv.score,items)}
function bots(){const order=['BTC','ETH','SOL','XRP','HBAR','SUI','ADA','PEPE','AVAX'];const groups=order.map(s=>assetGroup(s,state.bots.filter(b=>b.symbol===s))).join('');return `<section class="hero bot-hero"><div class="eyebrow">BOT CONTROL CENTER · ${state.source}</div><h1>PIONEX COIN-M</h1><p class="muted">${state.bots.length} aktive Long-Bots · ${new Set(state.bots.map(b=>b.symbol)).size} Assets · nach Asset gruppiert</p></section>${regimeStrip()}${exposureStrip()}${hedgeCard()}${groups}`}
function market(){const i=state.intel;return `<section class="hero"><div class="eyebrow">MARKET + BODEN</div><h1>BTC REGIME</h1><p class="muted">1D entscheidet · 4h timet · OKX USDT-SWAP · Binance fallback</p></section><div class="grid"><section class="metric"><span>REGIME</span><b>${state.market||'SYNC'}</b><small>${i?money(i.price):'Market sync'}</small></section><section class="metric"><span>RSI</span><b>${i?i.rsi4.toFixed(1)+' · '+i.rsi1.toFixed(1):'SYNC'}</b><small>4h · 1D</small></section><section class="metric"><span>MACD HIST</span><b>${i&&i.macd4?i.macd4.hist.toFixed(1):'SYNC'}</b><small>4h momentum</small></section><section class="metric"><span>EMA 20 / 50</span><b>${i?money(i.ema20)+' / '+money(i.ema50):'SYNC'}</b><small>1D trend</small></section><section class="metric"><span>EMA 100 / 200</span><b>${i?money(i.ema100)+' / '+money(i.ema200):'SYNC'}</b><small>1D structure</small></section><section class="metric"><span>NEAREST FIB</span><b>${i?i.near.f.toFixed(3)+' · '+money(i.near.price):'SYNC'}</b><small>90 × 4h swing</small></section><section class="metric"><span>ATR 4H</span><b>${i?money(i.atr):'SYNC'}</b><small>Range width input</small></section><section class="metric"><span>NEXT RANGE SCORE</span><b class="${i&&i.status==='RE-ENTRY READY'?'tone-safe':'wait'}">${i?i.score+'/100':'SYNC'}</b><small>${i?i.status:'loading'}</small></section></div>`}
function research(){return `<section class="hero"><div class="eyebrow">RESEARCH</div><h1>NEXT RANGE LAB</h1><p class="muted">Keine Auswirkung auf echte Pionex-Bots.</p></section><section class="card"><b>BTC NEXT RANGE / COMPOUND V1</b><p>TP → Reload Reserve → WAIT FOR RETRACE → bestätigter Re-Entry.</p><small>Fib · RSI · MACD · EMA · ATR · Walk-forward / Holdout</small></section>`}
function more(){return `<section class="hero"><div class="eyebrow">MORE</div><h1>PORTFOLIO + SYSTEM</h1><p class="muted">Spot/Exchanges bleiben sekundär. Live-Quelle: ${state.source}.</p></section>`}
const render={command,bots,market,research,more};let current='command';
function go(v){current=v;document.querySelectorAll('.view').forEach(x=>x.classList.toggle('active',x.id==='view-'+v));document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.v===v));$('#view-'+v).innerHTML=render[v]()}
async function sync(){try{const payload=await getJson('/api/private/dashboard'),d=payload?.data||payload,live=Array.isArray(d?.pionexRisk?.bots)?d.pionexRisk.bots.map(normalizeLive):[];state.bots=live.length?mergeReference(live):FALLBACK;state.source=live.length?'LIVE':'REFERENCE';state.market=String(d?.market?.regime||d?.btcRegime?.label||d?.regime?.label||'SYNC').toUpperCase();state.portfolio=portfolioModel(d);state.error=null}catch(e){state.error=e.message;state.source='REFERENCE'}go(current)}
document.querySelectorAll('#nav button').forEach(b=>b.onclick=()=>go(b.dataset.v));go('command');Promise.all([sync(),syncIntel()]).then(()=>go(current));setInterval(sync,30000);setInterval(()=>syncIntel().then(()=>{if(current==='market')go(current)}),60000);
