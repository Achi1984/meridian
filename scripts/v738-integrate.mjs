import fs from 'node:fs';
const must=(s,a,b,label)=>{if(!s.includes(a))throw new Error('missing '+label);return s.replace(a,b)};

let server=fs.readFileSync('server.js','utf8');
server=must(server,'import { runCloudBacktest } from "./cloud-backtest.js";','import { runCloudBacktest } from "./cloud-backtest.js";\nimport { regimeDecision, REGIME_V1_RULESET, REGIME_V1_CONFIG } from "./regime-v1.js";','server regime import');
const obs='try{await observeChallengerV2Scan(candidates);}catch(e){await addEvent("CHALLENGER_V2_ERROR",{stage:"observe",message:String(e?.message||e)}).catch(()=>{});}const ready=';
server=must(server,obs,'try{await observeChallengerV2Scan(candidates);}catch(e){await addEvent("CHALLENGER_V2_ERROR",{stage:"observe",message:String(e?.message||e)}).catch(()=>{});}try{await observeRegimeV1Scan(candidates);}catch(e){await addEvent("REGIME_V1_ERROR",{stage:"observe",message:String(e?.message||e)}).catch(()=>{});}for(const c of candidates){try{await submitRegimeV1(c);}catch(e){await addEvent("REGIME_V1_ERROR",{stage:"signal",symbol:c.symbol,message:String(e?.message||e)}).catch(()=>{});}}const ready=','server scan hook');
const cyc='try{await challengerV2Cycle(m);}catch(e){await addEvent("CHALLENGER_V2_ERROR",{stage:"cycle",message:String(e?.message||e)}).catch(()=>{});}for(const c of closed)';
server=must(server,cyc,'try{await challengerV2Cycle(m);}catch(e){await addEvent("CHALLENGER_V2_ERROR",{stage:"cycle",message:String(e?.message||e)}).catch(()=>{});}try{await regimeV1Cycle(m);}catch(e){await addEvent("REGIME_V1_ERROR",{stage:"cycle",message:String(e?.message||e)}).catch(()=>{});}for(const c of closed)','server cycle hook');
server=must(server,'const [paper, evidence, events, db, shadowV1, challengerV2] = await Promise.all([','const [paper, evidence, events, db, shadowV1, challengerV2, regimeV1] = await Promise.all([','assistant destructure');
server=must(server,'    challengerV2Status()\n  ]);','    challengerV2Status(),\n    regimeV1Status()\n  ]);','assistant promise');
server=must(server,'    challengerV2,\n    events:(events||[]).slice(0,20)','    challengerV2,\n    regimeV1,\n    events:(events||[]).slice(0,20)','assistant result');
const marker='// MERIDIAN v6.88 — persistent Cloud Backtest Jobs (research-only)';
const block=`// MERIDIAN REGIME V1 — research-only adaptive strategy selector.\nconst REGIME_V1_KEY="regime_v1";\nconst REGIME_V1_START=10000;\nasync function loadRegimeV1(){const z=await getState(REGIME_V1_KEY,null);if(z)return rollover(z);const x={account:{startEquity:REGIME_V1_START,cash:REGIME_V1_START,equity:REGIME_V1_START,peakEquity:REGIME_V1_START,dayStartEquity:REGIME_V1_START,dayKey:today(),realizedPnl:0,unrealizedPnl:0},positions:[],trades:[],equityCurve:[{ts:new Date().toISOString(),equity:REGIME_V1_START}],lastSignal:null,lastDecision:null,lastEvaluations:[],createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),ruleset:REGIME_V1_RULESET};await saveRegimeV1(x);return x;}\nasync function saveRegimeV1(x){x.updatedAt=new Date().toISOString();await setState(REGIME_V1_KEY,x);}\nfunction regimeModel(signal){return regimeDecision(signal,{fullRiskPct:config.riskPerTradePct,cautionRiskPct:Math.max(.1,round(config.riskPerTradePct*.5,3))});}\nasync function observeRegimeV1Scan(candidates){const s=rollover(await loadRegimeV1()),obs=(candidates||[]).map(regimeModel).sort((a,b)=>b.score-a.score);s.lastEvaluations=obs.slice(0,10);s.lastDecision=obs[0]||null;s.lastScanAt=new Date().toISOString();await saveRegimeV1(s);return obs;}\nfunction openRegimePosition(signal,account,d){const entry=slip(+d.entry,d.side,true),dist=Math.abs(entry-(+d.sl)),qty=dist>0?account.equity*(d.riskPct/100)/dist:0;if(!(qty>0))throw new Error("regime position size <=0");const fee=qty*entry*config.feeBps/10000;return{id:crypto.randomUUID(),symbol:String(signal.symbol||"").toUpperCase(),side:d.side,status:"OPEN",entry,qty,sl:+d.sl,tp1:+d.tp1||null,tp2:+d.tp2||null,openedAt:new Date().toISOString(),riskPct:d.riskPct,feeOpen:fee,source:"MERIDIAN-REGIME-V1",ruleset:REGIME_V1_RULESET,technical:signal.technical??null,candidate:signal.candidate??null,distanceAtr:d.distanceAtr??null,regimeType:d.regime,regimeScore:d.score,regimeDecision:d.decision,sourceSide:d.sourceSide,entryQuality:d.entryQuality,momentumFit:d.momentumFit};}\nasync function submitRegimeV1(signal){const s=rollover(await loadRegimeV1()),d=regimeModel(signal);s.lastSignal={...d,receivedAt:new Date().toISOString()};if(!["TRADE","CAUTION"].includes(d.decision)){await saveRegimeV1(s);return{accepted:false,reasons:d.reasons,decision:d};}if(s.positions.some(p=>p.status==="OPEN"&&p.symbol===d.symbol))return{accepted:false,reasons:["SAME_SYMBOL_POSITION_OPEN"],decision:d};const cutoff=Date.now()-config.cooldownMinutes*60000;if(s.trades.some(t=>t.symbol===d.symbol&&new Date(t.closedAt||t.openedAt||0).getTime()>=cutoff))return{accepted:false,reasons:["SYMBOL_COOLDOWN"],decision:d};const tradeSignal={...signal,symbol:d.symbol,side:d.side,entry:d.entry,sl:d.sl,tp1:d.tp1,tp2:d.tp2};const gate=challengerRiskGate({account:s.account,positions:s.positions.filter(p=>p.status==="OPEN"),tradesToday:dayTradeCount(s),signal:tradeSignal,riskPct:d.riskPct});if(!gate.ok){s.lastSignal.gate=gate;await saveRegimeV1(s);return{accepted:false,reasons:gate.reasons,gate,decision:d};}const p=openRegimePosition(signal,s.account,d);s.positions.push(p);s.account.cash-=p.feeOpen;s.account.realizedPnl-=p.feeOpen;await saveRegimeV1(s);await addEvent("REGIME_V1_POSITION_OPENED",{id:p.id,symbol:p.symbol,side:p.side,regime:p.regimeType,score:p.regimeScore,riskPct:p.riskPct,entry:p.entry,sl:p.sl,tp1:p.tp1});return{accepted:true,position:p,gate,decision:d};}\nasync function regimeV1Cycle(m){let s=rollover(await loadRegimeV1());const next=[],closed=[];let unreal=0;for(const p of s.positions){if(p.status!=="OPEN")continue;const q=m.quotes[p.symbol];if(!q||Date.now()-q.ts>config.marketStaleMs){next.push(p);continue;}const marked=markPosition(p,q.price),reason=exitReason(marked,q.price);if(reason){const done=closePaperPosition(marked,q.price,reason);done.ruleset=REGIME_V1_RULESET;done.regimeType=p.regimeType;done.regimeScore=p.regimeScore;s.account.cash+=done.realized+p.feeOpen;s.account.realizedPnl+=done.realized+p.feeOpen;s.trades.push(done);closed.push(done);}else{unreal+=marked.unrealized;next.push(marked);}}s.positions=next;s.account.unrealizedPnl=unreal;s.account.equity=s.account.cash+unreal;s.account.peakEquity=Math.max(s.account.peakEquity||s.account.equity,s.account.equity);const le=last(s.equityCurve);if(!le||Date.now()-new Date(le.ts).getTime()>=60000){s.equityCurve.push({ts:new Date().toISOString(),equity:s.account.equity});if(s.equityCurve.length>50000)s.equityCurve=s.equityCurve.slice(-50000);}await saveRegimeV1(s);for(const c of closed)await addEvent("REGIME_V1_POSITION_CLOSED",{symbol:c.symbol,side:c.side,regime:c.regimeType,score:c.regimeScore,riskPct:c.riskPct,realized:round(c.realized,2),exitReason:c.exitReason});}\nasync function regimeV1Status(){const s=await loadRegimeV1(),a=s.account||{},tr=s.trades||[],op=(s.positions||[]).filter(x=>x.status==="OPEN"),wins=tr.filter(x=>Number(x.realized)>0),losses=tr.filter(x=>Number(x.realized)<0),gp=wins.reduce((q,x)=>q+Number(x.realized||0),0),gl=Math.abs(losses.reduce((q,x)=>q+Number(x.realized||0),0)),peak=Number(a.peakEquity||a.equity||REGIME_V1_START),eq=Number(a.equity||REGIME_V1_START),dd=peak>0?Math.max(0,(peak-eq)/peak*100):0;return{enabled:true,researchOnly:true,independentLedger:true,executionImpact:false,ruleset:REGIME_V1_RULESET,model:{strategySelector:true,modes:["TREND_UP","TREND_DOWN","RANGE","EXPANSION","CHOP"],tradeScore:REGIME_V1_CONFIG.tradeScore,cautionScore:REGIME_V1_CONFIG.cautionScore,weights:REGIME_V1_CONFIG.weights,riskMultipliers:REGIME_V1_CONFIG.riskMultipliers},account:{startEquity:round(a.startEquity,2),cash:round(a.cash,2),equity:round(eq,2),peakEquity:round(peak,2),realizedPnl:round(a.realizedPnl,2),unrealizedPnl:round(a.unrealizedPnl,2),drawdownPct:round(dd,2)},openPositions:op,openCount:op.length,closedCount:tr.length,winRate:tr.length?round(wins.length/tr.length*100,1):0,profitFactor:gl>0?round(gp/gl,2):(gp>0?99:0),recentClosed:tr.slice(-12).reverse(),lastSignal:s.lastSignal||null,lastDecision:s.lastDecision||null,lastEvaluations:s.lastEvaluations||[],lastScanAt:s.lastScanAt||null,updatedAt:s.updatedAt};}\n\n`;
server=must(server,marker,block+marker,'regime block marker');
server=must(server,'"/api/shadow-v1","/api/challenger-v2"]','"/api/shadow-v1","/api/challenger-v2","/api/regime-v1"]','endpoint list');
server=must(server,'if(req.method==="GET"&&u.pathname==="/api/challenger-v2")return send(res,200,await challengerV2Status());','if(req.method==="GET"&&u.pathname==="/api/challenger-v2")return send(res,200,await challengerV2Status());if(req.method==="GET"&&u.pathname==="/api/regime-v1")return send(res,200,await regimeV1Status());','regime endpoint');
fs.writeFileSync('server.js',server);

let cb=fs.readFileSync('cloud-backtest.js','utf8');
cb=must(cb,"const BINANCE='https://api.binance.com';","import { regimeDecision } from './regime-v1.js';\nconst BINANCE='https://api.binance.com';",'cloud import');
cb=must(cb,"return{symbol,side,technical,candidate:cand,regime:regime(f),status,entry:price,sl,tp1,tp2,distanceAtr:dAtr}","return{symbol,side,technical,candidate:cand,regime:regime(f),status,entry:price,sl,tp1,tp2,distanceAtr:dAtr,frames:f}",'cloud frames');
cb=must(cb,"challenger:makeLedger('CHALLENGER_V2',cfg)},lastPrices={}","challenger:makeLedger('CHALLENGER_V2',cfg),regime:makeLedger('REGIME_V1',cfg)},lastPrices={}",'cloud ledger');
cb=must(cb,"if(cd.decision==='TRADE'||cd.decision==='CAUTION'){const L=ledgers.challenger,g=gate(L,sig,cd.riskPct,ev.t,cfg);if(g.ok)openPosition(L,sig,cd.riskPct,ev.t,{modelDecision:cd.decision,confidence:cd.confidence},cfg)}}","if(cd.decision==='TRADE'||cd.decision==='CAUTION'){const L=ledgers.challenger,g=gate(L,sig,cd.riskPct,ev.t,cfg);if(g.ok)openPosition(L,sig,cd.riskPct,ev.t,{modelDecision:cd.decision,confidence:cd.confidence},cfg)}const rd=regimeDecision(sig,{fullRiskPct:cfg.riskPerTradePct,cautionRiskPct:Math.max(.1,round(cfg.riskPerTradePct*.5,3))});if(rd.decision==='TRADE'||rd.decision==='CAUTION'){const rs={...sig,side:rd.side,entry:rd.entry,sl:rd.sl,tp1:rd.tp1,tp2:rd.tp2};const L=ledgers.regime,g=gate(L,rs,rd.riskPct,ev.t,cfg);if(g.ok)openPosition(L,rs,rd.riskPct,ev.t,{modelDecision:rd.decision,regimeType:rd.regime,regimeScore:rd.score,sourceSide:rd.sourceSide},cfg)}}",'cloud regime decision');
cb=must(cb,"return{baseline:stats(ledgers.baseline,cfg),shadow:stats(ledgers.shadow,cfg),challenger:stats(ledgers.challenger,cfg)}","return{baseline:stats(ledgers.baseline,cfg),shadow:stats(ledgers.shadow,cfg),challenger:stats(ledgers.challenger,cfg),regime:stats(ledgers.regime,cfg)}",'cloud return');
cb=must(cb,"challenger=compactStats(r.challenger);\n    windows.push({i:i+1,from,to,...baseline,baseline,shadow,challenger});","challenger=compactStats(r.challenger),regime=compactStats(r.regime);\n    windows.push({i:i+1,from,to,...baseline,baseline,shadow,challenger,regime});",'continuous window');
cb=must(cb,"variantStability('CHALLENGER V2','challenger',windows,whole.challenger)\n    ]","variantStability('CHALLENGER V2','challenger',windows,whole.challenger),\n      variantStability('REGIME V1','regime',windows,whole.regime)\n    ]",'continuous variant');
cb=must(cb,"challenger:{...compactStats(ledgers.challenger),tradeList:ledgers.challenger.tradeList,equityCurve:ledgers.challenger.equityCurve,byAsset:assetBreakdown(ledgers.challenger.tradeList)}}","challenger:{...compactStats(ledgers.challenger),tradeList:ledgers.challenger.tradeList,equityCurve:ledgers.challenger.equityCurve,byAsset:assetBreakdown(ledgers.challenger.tradeList)},regime:{...compactStats(ledgers.regime),tradeList:ledgers.regime.tradeList,equityCurve:ledgers.regime.equityCurve,byAsset:assetBreakdown(ledgers.regime.tradeList)}}",'result regime ledger');
cb=cb.replace("version:'7.34-RESEARCH-V2'","version:'7.38-REGIME-V1'");
fs.writeFileSync('cloud-backtest.js',cb);

let gw=fs.readFileSync('server-gateway.js','utf8');
gw=must(gw,'"/api/shadow-v1","/api/challenger-v2","/api/backtests"','"/api/shadow-v1","/api/challenger-v2","/api/regime-v1","/api/backtests"','gateway protected');
gw=must(gw,'challenger:["CHALLENGER_V2_POSITION_OPENED","CHALLENGER_V2_POSITION_CLOSED"]','challenger:["CHALLENGER_V2_POSITION_OPENED","CHALLENGER_V2_POSITION_CLOSED"],\n    regime:["REGIME_V1_POSITION_OPENED","REGIME_V1_POSITION_CLOSED"]','activity defs');
gw=gw.replace('const commonStart=starts.length===3?Math.max(...starts):null;','const commonStart=starts.length===Object.keys(defs).length?Math.max(...starts):null;').replace('const commonEnd=ends.length===3?Math.min(...ends):null;','const commonEnd=ends.length===Object.keys(defs).length?Math.min(...ends):null;');
fs.writeFileSync('server-gateway.js',gw);

let hard=fs.readFileSync('app-v7.33-hardening.js','utf8');
hard=must(hard,"'/api/shadow-v1','/api/challenger-v2','/api/backtests'","'/api/shadow-v1','/api/challenger-v2','/api/regime-v1','/api/backtests'",'frontend protected');
hard=must(hard,"const b=w.ledgers.baseline||{}, sh=w.ledgers.shadow||{}, ch=w.ledgers.challenger||{};","const b=w.ledgers.baseline||{}, sh=w.ledgers.shadow||{}, ch=w.ledgers.challenger||{}, rg=w.ledgers.regime||{};",'activity regime data');
hard=must(hard,"${row('CHALLENGER V2',ch,'challenger')}\n      <div class=\"v732-note\">","${row('CHALLENGER V2',ch,'challenger')}\n      ${row('REGIME V1',rg,'regime')}\n      <div class=\"v732-note\">",'activity regime row');
fs.writeFileSync('app-v7.33-hardening.js',hard);

let idx=fs.readFileSync('index.html','utf8');
idx=must(idx,"new Set(['baseline','shadow','challenger','compare'])","new Set(['baseline','shadow','challenger','regime','compare'])",'paper allowed tabs');
fs.writeFileSync('index.html',idx);

let loader=fs.readFileSync('app-v6.06.js','utf8');
loader=must(loader,"document.write('<script src=\"app-v7.37-ui-polish.js?v=7.37-R1\"><\\/script>');","document.write('<script src=\"app-v7.37-ui-polish.js?v=7.37-R1\"><\\/script>');\n    document.write('<script src=\"app-v7.38-regime-ui.js?v=7.37-R1\"><\\/script>');",'loader sync path');
loader=must(loader,".then(()=>load('app-v7.37-ui-polish.js?v=7.37-R1'))",".then(()=>load('app-v7.37-ui-polish.js?v=7.37-R1'))\n    .then(()=>load('app-v7.38-regime-ui.js?v=7.37-R1'))",'loader async path');
fs.writeFileSync('app-v6.06.js',loader);

let sync=fs.readFileSync('scripts/release-sync.mjs','utf8');
sync=must(sync,"s=required(s,/app-v7\\.37-ui-polish\\.js\\?v=[^\"'<>\\s]+/g,`app-v7.37-ui-polish.js?v=${cacheTag}`,'ui polish loader tag');","s=required(s,/app-v7\\.37-ui-polish\\.js\\?v=[^\"'<>\\s]+/g,`app-v7.37-ui-polish.js?v=${cacheTag}`,'ui polish loader tag');\n  s=required(s,/app-v7\\.38-regime-ui\\.js\\?v=[^\"'<>\\s]+/g,`app-v7.38-regime-ui.js?v=${cacheTag}`,'regime ui loader tag');",'release sync regime');
fs.writeFileSync('scripts/release-sync.mjs',sync);

let check=fs.readFileSync('scripts/release-check.mjs','utf8');
check=must(check,"must(loader.includes(`app-v7.37-ui-polish.js?v=${tag}`),'ui polish loader cache tag mismatch');","must(loader.includes(`app-v7.37-ui-polish.js?v=${tag}`),'ui polish loader cache tag mismatch');\nmust(loader.includes(`app-v7.38-regime-ui.js?v=${tag}`),'regime UI loader cache tag mismatch');",'release check loader');
check=must(check,"must(uiPolish.includes('meridian-release-status-row'),'mobile status row missing');","must(uiPolish.includes('meridian-release-status-row'),'mobile status row missing');\nconst regimeUi=read('app-v7.38-regime-ui.js');\nmust(regimeUi.includes('/api/regime-v1'),'regime UI endpoint missing');\nmust(read('regime-v1.js').includes(\"REGIME_V1_RULESET='7.38-REGIME-V1'\"),'regime model ruleset missing');",'release check regime');
fs.writeFileSync('scripts/release-check.mjs',check);

const version=JSON.parse(fs.readFileSync('version.json','utf8'));version.version='7.38';version.ui='7.38';version.buildId='7.38-20260901-R1';version.regimeResearch='7.38-REGIME-V1';version.release='v7.38 Regime V1: independent research-only adaptive strategy selector with Trend, Range, Expansion and Chop modes; Baseline 6.2 execution unchanged';fs.writeFileSync('version.json',JSON.stringify(version)+'\n');
console.log('v7.38 integration applied');
