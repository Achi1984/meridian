# ACHI MERIDIAN v5.10.8

UI stability patch based on v5.10.7.

- Bot Priority Queue no longer duplicates identical master actions.
- BTC-S30 now renders `REDUCE / EXIT CHECK` once.
- BTC-L20 now renders `KEEP / NO ADD` once.
- If snapshot action differs from the master action, both remain visible.
- Expanded `<details>` state preservation remains unchanged.
- No changes to Hedge, Liquidation Guard, Entry Readiness, scoring, portfolio, or Pionex snapshot logic.
