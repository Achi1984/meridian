name: Volatility Compression Expansion V1 Evidence

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'vol-compression-expansion-v1.js'
      - 'scripts/vol-compression-expansion-v1-evidence.mjs'
      - 'test/vol-compression-expansion-v1.test.js'
      - 'research/vol-compression-expansion-v1-design.md'
      - '.github/workflows/vol-compression-expansion-v1-evidence.yml'

permissions:
  contents: read

jobs:
  evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify frozen contract
        run: node --test test/vol-compression-expansion-v1.test.js
      - name: Run historical evidence
        run: node scripts/vol-compression-expansion-v1-evidence.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: vol-compression-expansion-v1-evidence
          path: artifacts/vol-compression-expansion-v1-evidence.json
          if-no-files-found: error
