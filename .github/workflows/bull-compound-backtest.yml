name: Bull Compound Research Backtest

on:
  push:
    branches:
      - research/bull-compound-backtest-20260916
    paths:
      - research/bull_compound_backtest.py
      - .github/workflows/bull-compound-backtest.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run research backtest
        run: python research/bull_compound_backtest.py
      - name: Upload research result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bull-compound-backtest-result
          path: |
            bull-compound-result.json
            bull-compound-result.md
          if-no-files-found: warn
