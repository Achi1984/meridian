name: Breakout Trend V1 Evidence

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'breakout-trend-v1.js'
      - 'scripts/breakout-trend-v1-evidence.mjs'
      - 'test/breakout-trend-v1.test.js'
      - 'research/breakout-trend-v1-design.md'
      - '.github/workflows/breakout-trend-v1-evidence.yml'

permissions:
  contents: read

jobs:
  evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify frozen contract
        run: node --test test/breakout-trend-v1.test.js
      - name: Run historical evidence
        run: node scripts/breakout-trend-v1-evidence.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: breakout-trend-v1-evidence
          path: artifacts/breakout-trend-v1-evidence.json
          if-no-files-found: error
