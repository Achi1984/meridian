ACHI MERIDIAN v6.48 — EQUITY FILTER STABLE RERENDER

ROOT CAUSE FIX
- v6.47 changed only the selected button visually because it attempted to call a non-app render() function.
- The chart DOM therefore remained on the old 1D render while 1H looked selected.
- v6.48 re-renders the real daytrade/PAPER view via renderOne('daytrade').

TIMEFRAME SSOT
- CLOUD_EQUITY_RANGE is the only active timeframe state.
- 15M / 1H / 4H / 1D / 1W persist in sessionStorage.
- Every manual switch rebuilds chart + metadata + event markers from the same selected range.
- Live cloud re-renders continue to respect the selected range.

CHART LABELS
- Marker groups now use the expected eq-mark CSS class.
- Prevents black/unreadable event labels.
- ENTRY and EXIT labels both use collision spacing; all events remain visible as dots.
- Shorter windows keep more labels, 1D/1W are deliberately decluttered.

UNCHANGED
- Paper Engine 6.29.1
- Ruleset 6.2-SIGNAL-V1
- Signal / Entry / SL / TP / Risk logic
